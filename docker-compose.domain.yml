# Режим DEPLOY_MODE=domain: свой домен + автоматический HTTPS.
# Порты 80/443 обязательны — по ним Let's Encrypt проверяет владение доменом.
services:
  caddy:
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
